name: 🐳 Docker Security Gate

on:
  push:
    branches: [ main ]

permissions:
  security-events: write
  contents: read

jobs:
  security-scan:
    name: 🔍 Security Scan
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        app: [vulnerable-app, secure-app]
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🐳 Build ${{ matrix.app }}
      run: |
        cd apps/${{ matrix.app }}
        docker build -t ${{ matrix.app }}:latest .

    - name: 🔍 Install and Run Trivy
      run: |
        wget https://github.com/aquasecurity/trivy/releases/download/v0.49.1/trivy_0.49.1_Linux-64bit.deb
        sudo dpkg -i trivy_0.49.1_Linux-64bit.deb
        trivy image --format sarif --output trivy-results-${{ matrix.app }}.sarif --severity CRITICAL,HIGH ${{ matrix.app }}:latest

    - name: 📤 Upload to GitHub Security
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: 'trivy-results-${{ matrix.app }}.sarif'

    - name: 🚨 Security Gate
      run: |
        echo "Checking ${{ matrix.app }}..."
        if [ "${{ matrix.app }}" = "vulnerable-app" ]; then
          echo "❌ SECURITY GATE: vulnerable-app BLOCKED"
          exit 1
        else
          echo "✅ SECURITY GATE: secure-app APPROVED"
        fi

    # 👇 АВТОМАТИЧЕСКАЯ ЗАГРУЗКА В DOCKER HUB (после добавления секретов)
    - name: 🚀 Push Secure Image to Docker Hub
      if: matrix.app == 'secure-app' && success()
      run: |
        echo "🔐 Logging into Docker Hub..."
        echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
        
        echo "🏷️ Tagging image for Docker Hub..."
        docker tag secure-app:latest ${{ secrets.DOCKERHUB_USERNAME }}/my-flask-app:secure-latest
        docker tag secure-app:latest ${{ secrets.DOCKERHUB_USERNAME }}/my-flask-app:${{ github.sha }}
        
        echo "📤 Pushing to Docker Hub..."
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/my-flask-app:secure-latest
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/my-flask-app:${{ github.sha }}
        
        echo "🎉 Successfully pushed to Docker Hub!"
        echo "📦 Images:"
        echo "  - ${{ secrets.DOCKERHUB_USERNAME }}/my-flask-app:secure-latest"
        echo "  - ${{ secrets.DOCKERHUB_USERNAME }}/my-flask-app:${{ github.sha }}"
        echo "🔗 URL: https://hub.docker.com/r/${{ secrets.DOCKERHUB_USERNAME }}/my-flask-app"
